= simple_form_for @balance, :html => { :class => 'form-horizontal' } do |f|
  = f.input :nom
  = f.input :montant
  = f.input :fini
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), balances_path, :class => 'btn'
