= simple_form_for @monnaie, :html => { :class => 'form-horizontal' } do |f|
  = f.input :iso
  = f.input :pourcent_vers_cad
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), monnaies_path, :class => 'btn'
